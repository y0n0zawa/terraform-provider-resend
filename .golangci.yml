version: "2"

linters:
  default: standard
  enable:
    - gosec
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gocritic
    - misspell
    - nilerr
    - unparam

formatters:
  enable:
    - goimports
